# Frontend Structure

This folder is separated from the backend and contains web client assets.

## Current layout

- `public/index.html`: main responsive web application entry page.

## Notes

- Backend serves this folder as static content.
- If you later migrate to React/Vite/Next, keep source code under `src/` and build output under `public/` or `dist/`.
